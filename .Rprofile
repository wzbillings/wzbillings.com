source("~/.Rprofile")
Sys.setenv(RSTUDIO_PKG_MANAGER_ENABLED = "0")
source("renv/activate.R")
# options(repos =  c(CRAN = "https://cloud.r-project.org",
# 				   stan = "https://mc-stan.org/r-packages/"))

options(repos =  c(CRAN = "https://cloud.r-project.org"))
